{
  "name": "drinq-infra",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "synth": "cdk synth -c stage=stg",
    "deploy:stg": "cdk deploy -c stage=stg",
    "deploy:prod": "cdk deploy -c stage=prod",
    "destroy:stg": "cdk destroy -c stage=stg",
    "destroy:prod": "cdk destroy -c stage=prod"
  },
  "devDependencies": {
    "aws-cdk": "^2.0.0",
    "aws-cdk-lib": "^2.0.0",
    "constructs": "^10.0.0",
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0",
    "ts-node": "^10.0.0",
    "source-map-support": "^0.5.0"
  }
}
